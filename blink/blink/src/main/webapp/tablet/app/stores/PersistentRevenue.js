synchronicity.stores.persistentRevenues = new Ext.data.Store({
    model: 'synchronicity.models.PersistentRevenue'
});

Ext.regStore('synchronicity.stores.persistentRevenues', synchronicity.stores.persistentRevenues);