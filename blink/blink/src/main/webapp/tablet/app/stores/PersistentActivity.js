synchronicity.stores.persistentActivities = new Ext.data.Store({
    model: 'synchronicity.models.PersistentActivity'
});

Ext.regStore('synchronicity.stores.persistentActivities', synchronicity.stores.persistentActivities);