synchronicity.stores.persistentProducts = new Ext.data.Store({
    model: 'synchronicity.models.PersistentProduct'
});

Ext.regStore('synchronicity.stores.persistentProducts', synchronicity.stores.persistentProducts);