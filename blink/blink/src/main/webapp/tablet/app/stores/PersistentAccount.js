synchronicity.stores.persistentAccounts = new Ext.data.Store({
    model: 'synchronicity.models.PersistentAccount'
});

Ext.regStore('synchronicity.stores.persistentAccounts', synchronicity.stores.persistentAccounts);