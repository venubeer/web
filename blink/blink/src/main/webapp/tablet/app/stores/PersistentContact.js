synchronicity.stores.persistentContacts = new Ext.data.Store({
    model: 'synchronicity.models.PersistentContact'
});

Ext.regStore('synchronicity.stores.persistentContacts', synchronicity.stores.persistentContacts);